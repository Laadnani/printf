#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <unistd.h>
#include <stdarg.h>
#include <unistd.h>
int _putchar(char c);
void _puts(char * str);
int _printf(char *format, ...);
void printnum(int num);

#endif
