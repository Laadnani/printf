#ifndef MAIN_H
#define MAIN_H

#include <limits.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdarg.h>
#include <unistd.h>
int _putchar(char c);
int _puts(char * str);
int _printf(const char *format, ...);
int printnum(int num);
int _printchar(char c);
int _printstr(char *str);
#endif
